driver:
  name: proxy
  host: localhost
  reset_command: "exit 0"
  port: <%= ENV["winrm_port"] %>

provisioner:
  name: chef_zero

platforms:
  - name: windows-2016

verifier:
  name: inspec

suites:
  - name: default
    run_list:
      - recipe[test::default]
